import { Link } from "react-router-dom";
import { useEffect, useState } from "react";
import Navbar from "@/components/Navbar";
import Footer from "@/components/Footer";
type R={slug:string;title:string;excerpt:string;content:string;image:string;createdAt:string};
export default function Realizations(){const [items,setItems]=useState<R[]>([]);useEffect(()=>{fetch("/api/realizations").then(r=>r.json()).then(setItems)},[]);return <><Navbar/><main className="min-h-screen bg-background text-foreground p-8 pt-32"><header className="text-center mb-14"><p className="text-primary text-xs tracking-[.3em] uppercase mb-4">Efekty naszej pracy</p><h1 className="text-4xl md:text-6xl font-heading">Nasze <span className="text-gradient-gold">Realizacje</span></h1></header><div className="max-w-6xl mx-auto grid md:grid-cols-3 gap-8">{items.map(x=><Link to={`/realizacje/${x.slug}`} key={x.slug} className="group border gold-border bg-card overflow-hidden card-shadow"><img src={x.image} className="w-full aspect-[4/3] object-cover group-hover:scale-105 transition-transform duration-500"/><div className="bg-white p-5"><h2 className="text-2xl font-heading text-zinc-900">{x.title}</h2><p className="mt-3 text-zinc-600">{x.excerpt}</p></div></Link>)}</div></main><Footer/></>}
